# 混乱大枪战 · Web 版 —— 产品需求文档（PRD）

> 一款模仿 4399《混乱大枪战》的横版 2D 枪战网页小游戏。
> 纯前端实现，托管于 GitHub，通过 GitHub Pages 发布。

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 文档状态 | 草稿（待评审） |
| 最近更新 | 2026-09-19 |
| 目标平台 | 桌面浏览器（Chrome / Edge / Firefox） |
| 发布方式 | GitHub 仓库 + GitHub Pages |
| 技术定位 | 纯前端、无后端、无账号、单机 |

---

## 1. 项目概述

### 1.1 一句话描述

一款在浏览器里直接玩的横版 2D 枪战小游戏：玩家用 **WASD 控制角色移动与跳跃**，用**鼠标瞄准并射击**，在一张张关卡里清剿不断涌来的敌人，风格轻松、上手快，模仿 4399 小游戏《混乱大枪战》。

### 1.2 项目背景与目标

- **背景**：想做一款经典 4399 风格的网页休闲枪战游戏，作为个人前端项目练手，并公开发布给别人试玩。
- **核心目标**：
  1. 做出一个**可玩、好玩、能跑满帧**的横版枪战 MVP。
  2. 代码结构清晰，**0 基础也能逐步扩展**（加武器、加关卡、加敌人）。
  3. **零成本部署**：借助 GitHub Pages，任何人打开链接即可玩，无需安装。

### 1.3 非目标（本期明确不做）

为了让第一版能顺利完成，以下内容**明确不在本期范围**（列入未来规划）：

- ❌ 多人联机 / 联网对战（需要服务器，GitHub Pages 无法支持）
- ❌ 账号系统 / 云端存档 / 排行榜后端
- ❌ 移动端触屏适配（第一版只保证桌面键鼠体验）
- ❌ 内购、广告、任何商业化模块
- ❌ 复杂剧情、过场动画

---

## 2. 目标用户与场景

| 维度 | 说明 |
|------|------|
| 目标用户 | 喜欢休闲小游戏的普通网页访客；作者本人练手 |
| 使用设备 | 带键盘鼠标的电脑（台式 / 笔记本） |
| 使用场景 | 摸鱼、课间、通过分享链接随手玩几关 |
| 单局时长 | 单关 2~5 分钟，一局 10~20 分钟 |
| 上手难度 | 极低：进入即玩，无需教程也能摸清 WASD + 鼠标 |

---

## 3. 玩法设计

### 3.1 核心玩法循环

```
进入关卡 → 移动/跳跃走位 → 瞄准射击消灭敌人 → 清完当前波次/到达终点
        → 拾取掉落（弹药/血/武器） → 进入下一关 → 通关或阵亡结算 → 重开
```

### 3.2 视角与场景

- **视角**：横版 2D 平台视角（类似马里奥 / 混乱大枪战），角色在有地面、平台的关卡里左右移动、上下跳跃。
- **重力**：角色受重力影响，会下落、站在平台上、从平台边缘掉落。
- **镜头**：摄像机横向（必要时纵向）跟随主角，主角大致保持在屏幕中心区域。

### 3.3 操作方式（核心）

| 输入 | 操作 | 说明 |
|------|------|------|
| `A` / `D` | 左移 / 右移 | 水平移动 |
| `W` 或 `空格` | 跳跃 | 站在地面/平台时可跳；可选二段跳（未来） |
| `S` | 下蹲 / 下穿单向平台 | 视关卡设计而定 |
| 鼠标移动 | 瞄准 | 角色/枪口朝向鼠标方向 |
| 鼠标左键 | 开火 | 按住连发（视武器） |
| 鼠标右键 | 换弹 / 副功能 | 可选 |
| `R` | 手动换弹 | |
| `1` / `2` / `3` | 切换武器 | |
| `Esc` / `P` | 暂停 | |

> 射击方向 = 从角色中心指向鼠标位置的向量，与角色移动方向解耦（可以边往左跑边往右打）。

### 3.4 角色（主角）

| 属性 | 初始值（建议，可调） |
|------|------|
| 生命值 HP | 100 |
| 移动速度 | 中等 |
| 跳跃高度 | 约 2~3 个身位 |
| 无敌帧 | 受伤后 0.5s 短暂无敌，防连续掉血 |

### 3.5 武器系统

第一版内置 **3 种武器**，通过数字键或拾取切换：

| 武器 | 特点 | 弹药 | 备注 |
|------|------|------|------|
| 手枪（默认） | 单发、中等伤害、无限弹药 | ∞ | 保底武器 |
| 冲锋枪 | 连发、伤害低、弹速快 | 有限，可拾取补充 | 扫射手感 |
| 霰弹枪 | 一次多发扇形、近距离高伤 | 有限 | 近战爆发 |

武器数值建议参数（供开发时填表）：

| 参数 | 手枪 | 冲锋枪 | 霰弹枪 |
|------|------|--------|--------|
| 单发伤害 | 25 | 12 | 15×6 |
| 射速（发/秒） | 4 | 12 | 1.2 |
| 弹匣容量 | ∞ | 30 | 6 |
| 子弹速度 | 快 | 快 | 中 |
| 后坐/散布 | 小 | 中 | 大（扇形） |

### 3.6 敌人设计

第一版内置 **2 种基础敌人 + 1 个 Boss**：

| 敌人 | 行为 | HP | 攻击方式 |
|------|------|----|---------|
| 冲锋兵 | 朝玩家跑动、贴近后近战 | 40 | 接触伤害 |
| 枪手 | 保持距离、朝玩家射击 | 30 | 远程子弹 |
| Boss（关底） | 血厚、多段攻击、召唤小兵 | 500 | 弹幕 + 冲撞 |

敌人 AI 采用简单状态机：`待机 → 发现玩家 → 追击/开火 → 死亡`。

### 3.7 关卡与进程

采用**关卡制**（第一版）：

- 3~5 个手工设计的关卡，难度递增。
- 每关目标：消灭全部敌人 / 到达终点旗帜。
- 最后一关为 Boss 关。
- 通关后显示结算（用时、击杀数、评分）。

> 备选方案：**无尽波次模式**（在一张地图上一波波刷怪，比拼生存时间与分数）。第一版二选一，推荐先做关卡制，波次模式列入未来规划。

### 3.8 掉落与拾取

- 敌人死亡有概率掉落：**弹药包、回血包、武器**。
- 地图上可预置：血包、弹药箱、武器箱。

### 3.9 数值与难度

- 难度随关卡递增：敌人数量、HP、攻击频率上升。
- 全部核心数值集中在一个配置文件（如 `config.js`），方便调参。

---

## 4. 界面（UI）设计

### 4.1 界面清单

| 界面 | 内容 |
|------|------|
| 开始界面 | 游戏标题、【开始游戏】【操作说明】【设置】按钮 |
| 游戏内 HUD | 血条、当前武器与弹药、得分、关卡/波次、暂停按钮 |
| 暂停界面 | 继续、重新开始、返回主菜单 |
| 结算界面 | 胜利/失败、用时、击杀数、得分、最高分、【再来一局】 |
| 操作说明 | 图示 WASD + 鼠标操作 |

### 4.2 HUD 布局（示意）

```
┌────────────────────────────────────────────┐
│ HP ██████████░░  得分:1200   关卡 2/5    ⏸ │  ← 顶部
│                                              │
│                 [游戏画面区]                 │
│                                              │
│ 武器: 冲锋枪  弹药: 18/30                     │  ← 底部
└────────────────────────────────────────────┘
```

---

## 5. 美术与音效资源

### 5.1 资源获取策略

> 原则：**优先使用网上公开、可免费/可商用的素材；找不到合适的再用工具自行生成。所有素材必须记录来源与授权。**

推荐的免费素材来源（CC0 / 免费可商用为主）：

| 来源 | 说明 | 授权 |
|------|------|------|
| Kenney.nl | 大量高质量 2D 游戏素材（角色、枪、地块、UI） | CC0（完全免费） |
| OpenGameArt.org | 社区素材库，角色/敌人/特效/音效 | 混合（需逐项看授权） |
| itch.io（Free Assets） | 独立作者免费素材包 | 混合 |
| Freesound.org | 音效（枪声、爆炸、脚步） | 混合（多为 CC） |

⚠️ **合规要求**：每个采用的素材都要在 `assets/CREDITS.md` 里登记「文件名 / 作者 / 来源链接 / 授权协议」，避免版权问题。

### 5.2 需要的美术资源清单

| 类别 | 资源 |
|------|------|
| 主角 | 待机、奔跑、跳跃、射击、受伤、死亡 帧 |
| 敌人 | 冲锋兵、枪手、Boss 的动作帧 |
| 武器/子弹 | 3 种枪的图标、子弹、枪口火光 |
| 场景 | 地块（tile）、背景层、平台、旗帜/门 |
| 特效 | 击中火花、爆炸、掉落物图标 |
| UI | 按钮、血条、图标、字体 |

### 5.3 音效资源清单（可选，不阻塞第一版）

射击、换弹、跳跃、敌人受击/死亡、拾取、受伤、通关/失败、背景音乐（BGM）。

### 5.4 自行生成兜底

若找不到合适免费素材，用作图工具生成占位或正式素材（统一像素/卡通风格），同样登记到 CREDITS。

---

## 6. 技术方案

### 6.1 技术选型（面向 0 基础）

| 层 | 选择 | 理由 |
|----|------|------|
| 渲染 | **HTML5 Canvas 2D** | 浏览器原生，无需安装，学习门槛低 |
| 语言 | **原生 JavaScript（ES6+）** | 零构建，直接在浏览器运行 |
| 结构 | 纯静态文件（HTML/CSS/JS + 资源） | GitHub Pages 直接托管 |
| 存档 | **localStorage** | 存最高分、设置，纯前端可用 |
| 构建 | 无（可选后期引入） | 第一版保持"打开即跑" |

> 备选：若后期觉得手写太累，可引入轻量 2D 引擎 **Phaser 3**（通过 CDN 引入，仍是纯前端、可上 GitHub Pages）。第一版建议先用原生 Canvas 理解原理。

### 6.2 关键技术点

- **游戏主循环**：`requestAnimationFrame` 驱动，按 `deltaTime` 更新，保证不同帧率下速度一致。
- **物理**：自写简易 AABB 矩形碰撞 + 重力/跳跃；无需完整物理引擎。
- **输入**：监听 `keydown/keyup`（WASD）与 `mousemove/mousedown`（瞄准射击）。
- **瞄准**：`angle = atan2(mouseY - playerY, mouseX - playerX)`，子弹按该角度飞出。
- **实体管理**：玩家、敌人、子弹、掉落物统一用实体数组管理，逐帧更新与绘制。
- **资源加载**：进入游戏前预加载图片/音效，显示加载进度。

### 6.3 目标性能

- 桌面主流浏览器稳定 **60 FPS**。
- 首屏资源体积尽量小，加载快。

---

## 7. 仓库结构与发布

### 7.1 建议的仓库目录结构

```
chaos-shooter/
├── index.html          # 入口页面（GitHub Pages 会读它）
├── README.md           # 项目说明、玩法、在线试玩链接
├── LICENSE             # 代码开源协议（如 MIT）
├── css/
│   └── style.css
├── js/
│   ├── main.js         # 入口、主循环
│   ├── config.js       # 所有可调数值（武器/敌人/难度）
│   ├── input.js        # 键盘鼠标输入
│   ├── player.js       # 主角
│   ├── enemy.js        # 敌人与 AI
│   ├── weapon.js       # 武器与子弹
│   ├── level.js        # 关卡数据与加载
│   ├── ui.js           # HUD 与各界面
│   └── assets.js       # 资源预加载
└── assets/
    ├── images/         # 图片素材
    ├── audio/          # 音效
    └── CREDITS.md      # 素材来源与授权登记
```

### 7.2 GitHub Pages 发布流程

> 全程零成本、纯网页操作，适合 0 基础。

1. 在 GitHub 新建公开仓库（如 `chaos-shooter`）。
2. 把上面所有文件上传/推送到仓库（`index.html` 必须在根目录）。
3. 打开仓库 **Settings → Pages**。
4. Source 选择 **Deploy from a branch**，分支选 `main`，目录选 `/ (root)`，保存。
5. 等 1~2 分钟，页面会给出发布地址：`https://<你的用户名>.github.io/chaos-shooter/`。
6. 打开该地址即可在线试玩；之后每次推送代码，Pages 会自动更新。

⚠️ 注意事项：
- 所有资源路径用**相对路径**（如 `assets/images/player.png`），不要用 `/assets/...` 绝对路径，否则在 Pages 子目录下会 404。
- 仓库需为 **Public**（私有仓库的 Pages 需付费）。

---

## 8. 里程碑与迭代计划

| 阶段 | 目标 | 产出 |
|------|------|------|
| M0 骨架 | 页面能打开，Canvas 能画一个方块并用 WASD 移动 | 可运行空场景 |
| M1 核心操作 | 重力/跳跃/平台碰撞 + 鼠标瞄准射击 | 能跑能跳能开枪 |
| M2 战斗 | 敌人 AI + 受伤/死亡 + 血条 HUD | 一关能打 |
| M3 内容 | 3 种武器 + 2 种敌人 + 3~5 关 + Boss | 可完整通关 |
| M4 完善 | UI 界面、结算、音效、素材替换、localStorage | 体验完整的 MVP |
| M5 发布 | 上传 GitHub + 开启 Pages + 写 README | 公开可玩链接 |

---

## 9. 未来规划（本期不做）

- 无尽波次 / 生存模式
- 二段跳、冲刺、翻滚等进阶操作
- 更多武器、敌人、Boss、关卡
- 前端排行榜（结合免费 BaaS）或本地排行
- 移动端触屏适配
- 引入 Phaser 等引擎重构

---

## 10. 验收标准（第一版 MVP）

- [ ] 打开 GitHub Pages 链接即可进入游戏，无需安装。
- [ ] WASD 可移动与跳跃，重力与平台碰撞正常。
- [ ] 鼠标可瞄准，左键可射击，子弹朝鼠标方向飞出。
- [ ] 至少 3 种武器可切换，手感有区分。
- [ ] 至少 2 种敌人 + 1 个 Boss，有基础 AI，能被击杀。
- [ ] 有血条、弹药、得分、关卡 HUD。
- [ ] 能完整通关并进入结算界面，可再来一局。
- [ ] 桌面主流浏览器稳定运行（目标 60 FPS）。
- [ ] 所有第三方素材已在 CREDITS.md 登记来源与授权。

---

## 附录 A：术语表

| 术语 | 含义 |
|------|------|
| MVP | 最小可行产品，第一版能玩的核心版本 |
| HUD | 游戏中显示血量、弹药等信息的界面层 |
| AABB | 轴对齐包围盒，一种简单的矩形碰撞检测 |
| FPS | 每秒帧数，衡量流畅度 |
| CC0 | 一种"完全放弃版权"的授权，可免费商用 |
| tile | 拼接关卡地图的小方块地块 |
